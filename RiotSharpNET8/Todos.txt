// Method rate limiter needs to be implemented before implementing the RateLimitedRequester
// This needs to be done so the overall functionality of the RateLimitedRequester is extended and less work is needed in the endpoints

// HandleRequestFailure should be more deterministic in what went wrong, being it application rate limit or method rate limit. 
// Maybe a new exception type for this? See comments in RequesterBase.HandleRequestFailure for enum.

// RegionConverter needs to be updated to the new regions

// The rate limiter does not yet support proper RetryAfter.

// Make some default values for the different JSON properties????

// Update gamemodes (riotsharp.misc), Maybe also role

// Refactor the Summoner endpoint 

// Update the championStaticListWrapper to include whether it is fullData or not

// Update drakes, monsters and more